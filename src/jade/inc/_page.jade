mixin page(name, title)
    - CURRENT_PAGE = name
    doctype html
    html
        head
            link(href='/css/styles.css' rel='stylesheet')
        body
            include _header
            h1= title
            block
